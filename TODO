-Write test programs for libxfcegui4 and exo (verify that the ref/out/weak are correct, that memory is properly freed, ...)
-Write the .deps files for each package
-Add installation rules to the automake scripts
